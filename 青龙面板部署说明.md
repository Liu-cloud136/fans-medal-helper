# 青龙面板部署说明

## 🐉 青龙面板部署步骤

### 方法1：直接克隆到scripts目录

```bash
# 进入青龙面板容器
docker exec -it qinglong bash

# 进入脚本目录
cd /ql/data/scripts

# 克隆项目
git clone https://github.com/Liu-cloud136/fans-medal-helper

# 进入项目目录
cd fans-medal-helper

# 安装依赖
pip install -r requirements.txt

# 复制配置文件
cp users.example.yaml users.yaml

# 编辑配置文件
vi users.yaml  # 填入你的B站cookie等信息

# 运行测试
python3 index.py
```

### 方法2：在青龙面板中添加定时任务

1. **登录青龙面板**
2. **添加定时任务**：
   - 名称：`B站粉丝牌助手`
   - 命令：`cd /ql/data/scripts/fans-medal-helper && python3 index.py`
   - 定时规则：`0 1 * * *` (每天凌晨1点)

### 方法3：如果还是报错，手动创建目录

```bash
# 如果目录名不匹配，手动创建软链接
docker exec -it qinglong bash
cd /ql/data/scripts
ln -s fans-medal-helper fansMedalHelper

# 或者重新克隆到指定目录名
git clone https://github.com/Liu-cloud136/fans-medal-helper fansMedalHelper
cd fansMedalHelper
```

## 🔧 配置说明

编辑 `users.yaml` 文件：

```yaml
USERS:
  - cookie: "你的B站cookie"
    DAILY_TASK: true
    LIKE_TASK: true
    WATCH_TASK: true
    MAX_CONCURRENT_WATCH: 3
    NOTIFY_DETAIL: 1
    push_keys:
      # 配置你的推送方式（可选）
```

## 🚨 常见问题

### Q: 提示找不到 fansMedalHelper 目录？
A: 
```bash
# 创建软链接解决
cd /ql/data/scripts
ln -s fans-medal-helper fansMedalHelper
```

### Q: 提示找不到 index.py？
A: 确保 index.py 在项目根目录，或者直接运行：
```bash
cd /ql/data/scripts/fans-medal-helper
python3 main.py
```

### Q: 依赖安装失败？
A: 
```bash
# 升级pip
pip install --upgrade pip
# 安装依赖
pip install -r requirements.txt
```

## 📝 运行日志

查看运行日志：
```bash
# 查看青龙面板日志
docker logs qinglong

# 查看项目运行日志
tail -f /ql/data/scripts/fans-medal-helper/*.log
```