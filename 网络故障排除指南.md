# 网络故障排除指南

## 🚨 常见问题

### 1. 克隆失败：Connection reset by peer

**错误信息**：
```
fatal: unable to access 'https://mirror.ghproxy.com/https://github.com/Liu-cloud136/fans-medal-helper.git/': Recv failure: Connection reset by peer
```

**原因**：国内网络环境限制，GitHub访问受限

## 🔧 解决方案

### 方案1：切换镜像源

在Docker环境中设置不同的镜像源：

```bash
# 使用GitHub源（如果网络允许）
docker run -e MIRRORS=0 your-image

# 使用GHProxy镜像（默认）
docker run -e MIRRORS=1 your-image

# 使用FastGIT镜像
docker run -e MIRRORS=2 your-image

# 使用GHProxyCN镜像
docker run -e MIRRORS=3 your-image

# 使用GitClone镜像
docker run -e MIRRORS=4 your-image
```

### 方案2：使用自定义仓库

```bash
# 使用Gitee等国内镜像
docker run -e MIRRORS=custom -e CUSTOM_REPO=https://gitee.com/your-repo/fans-medal-helper.git your-image
```

### 方案3：本地部署（推荐）

如果网络问题严重，建议直接本地部署：

```bash
# 1. 手动下载项目
wget https://github.com/Liu-cloud136/fans-medal-helper/archive/refs/heads/master.zip
unzip master.zip
cd fans-medal-helper-master

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置文件
cp users.example.yaml users.yaml
vi users.yaml

# 4. 运行
python main.py
```

## 🌐 可用镜像源

| 镜像源 | 地址 | 说明 |
|--------|------|------|
| GitHub | `https://github.com/` | 官方源，需要网络环境 |
| GHProxy | `https://mirror.ghproxy.com/` | 常用镜像，可能不稳定 |
| FastGIT | `https://hub.fastgit.xyz/` | 备用镜像源 |
| GHProxyCN | `https://ghproxy.cn/` | 国内镜像源 |
| GitClone | `https://gitclone.com/` | 另一个镜像源 |

## 🔍 故障排查步骤

### 1. 测试网络连接

```bash
# 测试GitHub连接
curl -I https://github.com

# 测试镜像源连接
curl -I https://mirror.ghproxy.com
curl -I https://hub.fastgit.xyz
```

### 2. 检查DNS解析

```bash
# 检查GitHub域名解析
nslookup github.com
nslookup mirror.ghproxy.com
```

### 3. 配置代理（如有）

```bash
# 设置HTTP代理
export HTTP_PROXY=http://proxy-server:port
export HTTPS_PROXY=http://proxy-server:port

# Git配置代理
git config --global http.proxy http://proxy-server:port
git config --global https.proxy http://proxy-server:port
```

## 🚀 青龙面板特殊处理

### 方案1：修改定时任务命令

```bash
# 在青龙面板中设置不同的克隆方式
cd /ql/data/scripts
git clone https://hub.fastgit.xyz/Liu-cloud136/fans-medal-helper
# 或者
git clone https://gitee.com/mirrors/fans-medal-helper
```

### 方案2：直接下载文件

```bash
# 下载单文件
wget https://raw.githubusercontent.com/Liu-cloud136/fans-medal-helper/master/main.py
wget https://raw.githubusercontent.com/Liu-cloud136/fans-medal-helper/master/requirements.txt
```

## 📋 预防措施

### 1. 使用多个备用源

项目中已集成自动切换逻辑：
- GHProxy失败 → 切换到FastGIT
- FastGIT失败 → 切换到GitHub源

### 2. 重试机制

- 每次拉取最多重试3次
- 自动切换镜像源
- 失败后继续运行本地代码

### 3. 离线运行

一旦成功拉取，即使网络中断也可以继续运行本地代码。

## 🆘 紧急方案

如果所有方法都失败：

1. **手动上传文件**：直接上传项目文件到服务器
2. **使用CDN加速**：通过jsdelivr等CDN下载文件
3. **寻求帮助**：在GitHub Issues中反馈网络问题

---

**提示**：国内用户建议优先使用镜像源 `MIRRORS=2` 或 `MIRRORS=3`，稳定性相对较好。